// Shared visual constants for the General Ledger UI.

/** Primary header colour requested for modals / section headers. */
export const GL_HEADER = '#0369a1';

/** Tailwind class fragments reused across GL tables. */
export const GL = {
  headerBar: 'text-white', // used with inline GL_HEADER background
  tableHead:
    'bg-sky-50 text-[#0369a1] uppercase text-[11px] font-semibold tracking-wider',
  numeric: 'tabular-nums font-mono',
};
