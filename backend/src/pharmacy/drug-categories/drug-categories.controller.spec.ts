import { DrugCategoriesController } from './drug-categories.controller';

describe('DrugCategoriesController (pharmacy)', () => {
  it('is defined', () => {
    expect(new DrugCategoriesController()).toBeDefined();
  });
});
