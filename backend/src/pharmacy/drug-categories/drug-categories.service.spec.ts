import { DrugCategoriesService } from './drug-categories.service';

describe('DrugCategoriesService (pharmacy)', () => {
  it('is defined', () => {
    expect(new DrugCategoriesService()).toBeDefined();
  });
});
